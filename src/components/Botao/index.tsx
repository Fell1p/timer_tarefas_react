import React from 'react'; //importação do react em nosso codigo

class Botao extends React.Component { //criação de um componente
    render(): React.ReactNode {
        return (
            <button>
                Botão
            </button>
        )
    }
}

export default Botao; //Exportação do nosso componente